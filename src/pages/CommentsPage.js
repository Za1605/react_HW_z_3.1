import React from 'react';
import {Outlet} from "react-router-dom";

const CommentsPage = () => {
    return ( <div>
        commentsPage
        <Outlet/>
        </div> );
};

export default CommentsPage;